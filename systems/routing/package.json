{
  "name": "@place/routing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Client-side routing for @place/reactivity. v0.1 ships a hash-based router and a memoryRouter for tests, exposed as the Router capability so consumers can swap implementations without touching call sites.",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md",
    "docs"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
